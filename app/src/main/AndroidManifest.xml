<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Required for Pomodoro timer on newer Android versions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required for AlarmManager accurate alarms in Android 13+ -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <application
        android:name=".MyTaskListApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTaskList">

        <!-- ① Splash Screen — launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MyTaskList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ② Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Pomodoro Foreground Service -->
        <service
            android:name=".service.pomodoro.PomodoroForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|shortService" />

        <!-- Task Reminder Receiver -->
        <receiver android:name=".service.reminder.TaskReminderReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Deadline Warning Receiver -->
        <receiver android:name=".service.deadline.DeadlineWarningReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Summary Action Receiver -->
        <receiver android:name=".service.summary.SummaryActionReceiver" android:exported="false">
            <intent-filter>
                <action android:name="ACTION_SHOW_TODAY_SUMMARY" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
